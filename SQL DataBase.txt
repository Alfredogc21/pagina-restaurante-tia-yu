-------para copiar tal cual-----
CREATE TABLE roles (
    idRoles INT(11) PRIMARY KEY NOT NULL AUTO_INCREMENT,
    roles CHAR(30)
);

CREATE TABLE disponibilidades (
    idDisponibilidad INT(11) PRIMARY KEY NOT NULL AUTO_INCREMENT,
    estado VARCHAR(12) NOT NULL
);

CREATE TABLE accesos (
    idAcceso INT(11) PRIMARY KEY NOT NULL AUTO_INCREMENT,
    tipoAcceso INT(11) NOT NULL,
    descripcion VARCHAR(255)
);

CREATE TABLE mesa (
    idMesa INT(11) PRIMARY KEY NOT NULL AUTO_INCREMENT,
    numSillas INT(11),
    disponibilidad INT(11) NOT NULL,
    FOREIGN KEY (disponibilidad) REFERENCES disponibilidades(idDisponibilidad)
);

CREATE TABLE usuarios (
    idCliente INT(11) PRIMARY KEY NOT NULL AUTO_INCREMENT,
    cedula VARCHAR(20),
    p_Nombre CHAR(60) NOT NULL,
    s_Nombre CHAR(60),
    p_Apellido CHAR(60) NOT NULL,
    s_Apellido CHAR(60),
    idRoles INT(11), 
    idAccesos INT(11), 
    idDispon INT(11), 
    telefono VARCHAR(45) NOT NULL,
    correoElectronico VARCHAR(45) NOT NULL,
    password VARCHAR(255) NOT NULL,
    fechaRegistro DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL,
    FOREIGN KEY (idRoles) REFERENCES roles(idRoles),
    FOREIGN KEY (idAccesos) REFERENCES accesos(idAcceso),
    FOREIGN KEY (idDispon) REFERENCES disponibilidades(idDisponibilidad)
);

CREATE TABLE reserva (
    idReserva INT(11) PRIMARY KEY NOT NULL AUTO_INCREMENT,
    hora TIME,
    fecha DATE,
    comentario VARCHAR(100),
    numPersonas INT(11),
    idCliente INT(11),
    idMesa INT(11),
    FOREIGN KEY (idCliente) REFERENCES usuarios(idCliente),
    FOREIGN KEY (idMesa) REFERENCES mesa(idMesa)
);

CREATE TABLE valoracion (
    idValoracion INT(11) PRIMARY KEY NOT NULL AUTO_INCREMENT,
    comentario LONGTEXT,
    estrellas INT(11),
    idCliente INT(11),
    fechaValoracion DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL,
    FOREIGN KEY (idCliente) REFERENCES usuarios(idCliente)
);


INSERT INTO 'roles' ('idRoles', 'roles') VALUES (NULL, 'Administador'), (NULL, 'Empleado'), (NULL, 'Cliente');

INSERT INTO 'disponibilidades' ('idDisponibilidad', 'estado') VALUES (NULL, 'Activo'), (NULL, 'Inactivo'), (NULL, 'Desactivado');






-------insertar datos en tabla roles---------
INSERT INTO `roles` (`idRoles`, `roles`) VALUES (NULL, 'Administador'), (NULL, 'Empleado'), (NULL, 'Cliente');

-------insertar datos en tabla disponibilidades---------
INSERT INTO `disponibilidades` (`idDisponibilidad`, `estado`) VALUES (NULL, 'Activo'), (NULL, 'Inactivo'), (NULL, 'Desactivado');